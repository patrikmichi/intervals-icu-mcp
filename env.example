# Intervals.icu API Key (required)
# Generate at: https://intervals.icu -> Settings -> Developer Settings
INTERVALS_ICU_API_KEY=your_api_key_here

# Optional: Require clients to send this key to use the MCP (Authorization: Bearer <key> or X-MCP-API-Key)
# If set, only requests that send this key in headers are allowed.
# MCP_API_KEY=your_secret_mcp_key

# Optional: Webhook auth. If set, POST /api/webhook requires Authorization: Bearer <key> or X-Webhook-Secret.
# If unset, WEBHOOK_SECRET falls back to MCP_API_KEY; if neither is set, webhook accepts all requests.
# WEBHOOK_SECRET=your_webhook_secret

# Optional: Override base URL (defaults to https://intervals.icu/api/v1)
# INTERVALS_ICU_BASE_URL=https://intervals.icu/api/v1
